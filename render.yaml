services:
  - type: web
    name: botva-installer
    runtime: node
    plan: free
    region: frankfurt
    rootDir: installer
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
